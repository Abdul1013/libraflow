# Core — Python 3.13 compatible (pinned to resolved versions)
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.20

# ORM & Database
sqlmodel==0.0.38
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
alembic==1.18.4

# Validation & Settings
pydantic[email]==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0

# Security
python-jose[cryptography]==3.5.0
passlib[argon2]==1.7.4
argon2-cffi==25.1.0

# Cache
redis[hiredis]==5.0.4

# Intelligence (Sprint 3)
rapidfuzz==3.14.5
# scikit-learn>=1.4   — requires Python <3.14; install via conda or Docker
# pandas>=2.2         — same constraint

# Email Notifications
resend==0.7.2

# HTTP Client
httpx==0.27.0

# Dev
python-dotenv==1.2.2
